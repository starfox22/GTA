# Agent instructions

All working notes for AI coding agents (commands, rules, contracts, how to find code, how to
verify cheaply, how to publish) are in [CLAUDE.md](CLAUDE.md). Read it first; it is short.
Then `grep -i <topic> docs/FILEMAP.md` to find the files you need, and `docs/README.md` for
the per-area docs.
