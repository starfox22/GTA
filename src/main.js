      /**
       * Entry point
       * Source: src/main.js
       * Scope: the whole game closure.
       * Everything the game is lives inside this one function, spliced together by
       * tools/build.py from the @include directives in src/game.js. ASSETS carries
       * the decoded media handed over by src/asset-loader.js.
       */
      function startDeadEndCity(ASSETS) {
  // @include src/game.js
}
